class AStarHandler {

    val path = mutableListOf<Node>()

    // If the returned list is of size 0, then you are already at the goal tile
    fun getPathFromTo(start: Node, goal: Node, tiles: Array<Array<Node>>): List<Node> {
        path.clear()



        return path
    }
}